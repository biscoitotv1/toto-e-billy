# Performance

FPS alvo, resolução, limites de CPU/GPU, memória, otimização de assets,
carregamento, streaming, profiling e gargalos conhecidos: **A DEFINIR**.
Não há build ou código para medir no estado atual.
